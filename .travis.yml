addons:
  chrome: stable

sudo: false
language: javascript
branches:
  only:
    - master
cache:
  directories:
    - node_modules
install:
  - npm install
script:
  - npm test
  - echo "Add script to pack chrome extension"
